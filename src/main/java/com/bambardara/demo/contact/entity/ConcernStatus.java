package com.bambardara.demo.contact.entity;

/**
 * Where a submitted concern sits in the marketing team's queue.
 */
public enum ConcernStatus {

    NEW,
    IN_PROGRESS,
    RESOLVED,
    CLOSED
}
